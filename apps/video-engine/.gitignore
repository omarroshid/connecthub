node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Narration build intermediates (per-line synth + uncompressed wav) —
# only the final narration.mp3 + timeline.json are kept.
public/audio/lines
public/audio/narration.wav
